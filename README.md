# dashboard-project


![alt text](img/dashboard-project.png "Logo Title Text 1")




also to run test for jet javascript using docker write:

```bash
docker-compose up 
```

if you change the test or something write:
```bash
docker build -t dashboard-test .
```

and then:
```bash 
docker run dashboard-test
```